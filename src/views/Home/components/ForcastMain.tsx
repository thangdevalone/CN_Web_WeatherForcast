import * as React from 'react';
import { Box } from '@mui/material';
import classes from "./styles.module.css";
export function ForcastMain() {
    return <Box className={classes.forcastMain} >

    </Box>;
}
